// module 20.8

const computer = {
    Monitor : "view Sonic",
    mouse : 'walton',
    price : 15000,
    ssd : "512gb"
}

const key = Object.keys(computer); // all property in array feature
console.log(key);

const values = Object.values(computer); // all value in array feature
console.log(values);